import 'package:flutter/material.dart';

class ExploreArticle extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      child: Text('Explore Article'),
    );
  }
}
