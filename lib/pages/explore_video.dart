import 'package:flutter/material.dart';

class ExploreVideo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orange,
      child: Text('Explore Video'),
    );
  }
}